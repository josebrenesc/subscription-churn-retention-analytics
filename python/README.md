# Python folder

## Recommended order

1. Install dependencies:

```bash
pip install -r python/requirements.txt
```

2. Study `clean_subscription_data.py`.
3. Run `analysis_pipeline.py`.
4. Open `subscription_churn_analysis.ipynb` in VS Code or Jupyter.

The project uses synthetic data and requires no external API or confidential data.
